{
  "name": "dynamic-form-builder",
  "version": "1.0.0",
  "description": "Dynamic Form Builder with TypeScript",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "serve": "http-server -p 3000"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "http-server": "^14.0.0"
  }
}